<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hu.mybatis.mapper.BookMapper">

    <resultMap id="b" type="book">
<!--  id主键  column 字段名  property 变量名  -->
        <id column="id" property="id"/>
<!--        result 其他字段映射-->
        <result column="bookname" property="Name"/>
<!--        <association property="" javaType="">   association 类对象的映射 property 属性名  javaType 对象类型 -->
<!--            <id column="" javaType=""/>-->

<!--        </association>-->
<!--        <collection property="" javaType=""/>-->
    </resultMap>


    <select id="getName" resultType="String">
        select bookname from t_book
    </select>


<!--    public Book getBook(String name)-->
<!--    <select id="getBook" resultType="book">-->
<!--        select  * from t_book where bookname = #{name}-->
<!--    </select>-->
    <select id="getBook" resultMap="b">
        select  * from t_book where bookname = #{name}
    </select>
</mapper>